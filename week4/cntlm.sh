#!/bin/bash

#ps -ef is used to see every process on the system using standard syntax

#grep searches the named input file, by default grep prints the matching lines 

# Together the y show the process on the system that matches with the key word cntlm

ps -ef | grep cntlm
